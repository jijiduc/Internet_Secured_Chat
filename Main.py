"""""
C'est le fichier main, qui appelle toutes les fonctions et fait tourner le programme
"""""

from UI.button import *

#app = QApplication(sys.argv)
window = Ui_Dialog()
window.setupUi(Dialog: {objectName, resize, setWindowTitle})
#sys.exit(app.exec())